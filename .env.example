# Exemplo apenas — o app não exige este arquivo.
# Credenciais vão no keyring do sistema, não em variáveis de ambiente.

# Opcional: backend de keyring em ambientes headless/CI
# PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring
